{
  "name": "c1-dashboard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "c1/c1-dashboard",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/c1/c1-dashboard"
      },
      "configurations": {
        "development": {
          "outputPath": "c1/c1-dashboard"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "c1-dashboard:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "c1-dashboard:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "c1-dashboard:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "c1-dashboard:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "c1/c1-dashboard/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["c1/c1-dashboard/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
